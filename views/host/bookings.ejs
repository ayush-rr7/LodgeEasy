<!DOCTYPE html>
<html lang="en">

  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= pageTitle %> -LodgeEasy</title>
  <%- include('../partials/headerIcon')%>
 <link rel="stylesheet" href="/output.css" />
</head>
<body class="bg-gray-50 text-gray-800">
  <%- include('../partials/nav')%>
      <div id="taskbar">
        <% if(isLoggedIn){ %>
        
        <div class="max-w-6xl mx-auto px-4 py-10">
      <h1 class="text-2xl font-bold text-gray-800 mb-6">Your Bookings</h1>
         <% } %>  
  <% registeredHomes.forEach(function(booking) { %>
    <div class=" bg-gray-100 p-4 mb-4 rounded-lg">
      <P> You have a bookings</P>
      <h3>Booking ID: <%= booking._id %></h3>
      <!-- <p>Home ID: <%= booking.homeId %></p>
      <p>User ID: <%= booking.guestId %></p> -->
      <p>Check In : <%= booking.checkIn.toLocaleDateString('en-GB') %></p>
      <p>Check Out : <%= booking.checkOut.toLocaleDateString('en-GB') %></p>
      <p>Total Guests : <%= booking.numGuests %></p>
      <p>Total Price : ₹<%= booking.totalAmount %></p>
    </div>
    <% } ); %>
  
</body>
</html>