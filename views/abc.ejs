<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
       <link rel="stylesheet" href="output.css"/>
  </head>
  <body>
    <div class="max-w-xl mx-auto mt-20 p-8 bg-white rounded-xl shadow-lg text-center">
        
        <% if (typeof message !== 'undefined') { %>
            <p class="text-xl text-gray-700">
                <%= message %>
            </p>
        <% } else { %>
            <h1 class="text-6xl font-extrabold text-indigo-600 mb-2">404</h1>
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">Page Not Found</h2>
            <p class="text-gray-500">
                We couldn't find the URL you requested on LodgeEasy.
            </p>
        <% } %>

        <div class="mt-8">
            <a href="/" class="inline-block bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-3 px-6 rounded-lg transition">
                Return to Homepage
            </a>
        </div>
    </div>
  </body>
</html>
